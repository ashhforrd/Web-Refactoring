{
  "name": "@teamboard/api",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/server.ts",
    "start": "node dist/server.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@fastify/cookie": "^11.0.2",
    "@fastify/cors": "^10.0.1",
    "@teamboard/config": "workspace:*",
    "@teamboard/db": "workspace:*",
    "@teamboard/shared": "workspace:*",
    "fastify": "^5.3.2",
    "jose": "^6.0.11"
  },
  "devDependencies": { "tsx": "^4.19.4" }
}
